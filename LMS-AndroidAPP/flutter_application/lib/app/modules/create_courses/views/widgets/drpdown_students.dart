import 'package:flutter/material.dart';

import 'package:get/get.dart';

import '../../../../core/core_lib.dart';
import '../../controllers/create_courses_controller.dart';


class StudentsDropdown extends StatelessWidget {
  final CreateCoursesController controller;
  const StudentsDropdown({super.key, required this.controller});

  @override
  Widget build(BuildContext context) {
    return Container(
      // multi-selectable view
     );
  }
}
